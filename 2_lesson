<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <script>
    // 1       
      var a = 1, b = 1, c, d;
      c = ++a; alert(c);      // 2 потому что преикремент
      d = b++; alert(d);      // 1 потому что постинкремент
      c = (2+ ++a); alert(c); // 4 преинкремент 2 = 3 и + 2
      d = (2+ b++); alert(d); // 3 постинкремент 2 = 2 и + 2
      alert(a);               // 3 потому что 2 раза инкрементировали(без разницы пост или пре) выше,  
      alert(b);               // 3 потому что 2 раза инкрементировали(без разницы пост или пре) выше
    
    // 2
      var a = 2;
      var x = 1 + (a *= 2);
      alert(`x = ${x}`);      // 5
            
    //  3. Объявить две целочисленные переменные a и b и задать им произвольные начальные значения. Затем написать скрипт, который работает по следующему принципу:
    //     если a и b положительные, вывести их разность;
    //     если а и b отрицательные, вывести их произведение;
    //     если а и b разных знаков, вывести их сумму; ноль можно считать положительным числом.

    let a = 1, b = 1;
    if ((a >= 0) && (b >= 0))
      alert(`a - b = ${a - b}`);
    else if ((a < 0) && (b < 0))
      alert(`a * b = ${a * b}`);
    else
      alert(`a + b = ${a + b}`);

    </script>
</head>
<body>
</body>
</html>
